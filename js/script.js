

const swiper = new Swiper('.swiper', {
    // Optional parameters
    lazy: true,
    loop: true,

    // Navigation arrows
    navigation: {
        nextEl: '.slide-next',
        prevEl: '.slide-prev',
    },
});

